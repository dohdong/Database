테이블, 행 => 튜플 , 열 => 속성(attribute), 필드 


특별한 의미를 지닌 열, 키 

기본키(PK : Primary Key) 제일 중요
보조키(alternate key, 대체키)
슈퍼키(Super key) 유일성만 만족하면 됨..? 뭐지? // 나중에 검색해볼것.
외래키(FK : Foreign Key) 두번째로 중요. => 다른 tb&(table의 약자)의 기본키로 쓰이는 속성 
					( 아 아까 table끼리 연결시킬때 쓰이는 호봉같은 것.)
복합키(composite key) (속성 하나로 된 것이 아니라) 속성 두가지 이상으로 이루어진 키를 뜻함.
36p에서 보면 과목코드를 기본키로 하기에는 같은것이 있기때문에 과목코드와 담당교수 두개를 기본키로 즉, 복합키로 해야함.
(과목코드, 담당교수) 로 써주어야함.    -> 근데 이렇게 두개로 하면 단점이 많음. 그래서 잘 안씀.
이럴땐 그냥 새롭게 과목번호 열을 새로 만들어서 1,2,3... 이렇게 정해주는편. (-> 이 방법이 훨씬 쉬운듯.)


2. 오라클 데이터베이스.
현재는 19c까지 나왔다고 하는데 우리는 Oracle 11g 을 쓴다. 다른 어떤거랑 호환을 해도 문제가 없다. 안정적이다.


VARCHAR2(길이) = 4000byte만큼의 가변 길이 문자열 데이터를 저장(최소 크기는 1byte) ,(오라클에만 있는 자료형이 있음. )
NUMBER(전체 자릿수, 소수점 이하 자릿수)
DATE = 날짜, 뿐만 아니라 시,분,초 도 가능.

table .. 데이터를 저장하는 장소 = 개체.
index .. 요즘은 인덱스를 안 만드는 추세 (이유는 그 공간을 새로 할애해야하므로)
그외 여러 객체들이 있는데 솔직히 와닿지 않음. 뷰, 시퀀스, 프로시저, 함수 등등.
자바의 메서드와 오라클의 함수가 같은 개념. 이다.... 
메서드는 객체지향언어에서는 함수를 메서드라고 부름 함수랑 메서드랑 비슷.. 
메서드(객체지향 언어), 함수(절차지향 언어) C언어는 절차지향언어이므로 함수가 있음.



PL/SQL
Procedural Language extension to SQL























